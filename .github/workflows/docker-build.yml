name: Build and Push All MLOps Images

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      fail-fast: false
      matrix:
        include:
          - service_name: backend
            context: ./api
            dockerfile: ./api/Dockerfile.backend
            image_suffix: backend
            tag_suffix: "latest"
            filter_pattern: |
              src:
                - 'api/**'
                - '!api/Dockerfile.builder'
                - '!api/Dockerfile.base'
                - '!api/deploy_worker.py'
                - '!api/service.py'
          - service_name: training-blackwell
            context: ./training
            dockerfile: ./training/Dockerfile
            image_suffix: training
            cuda_ver: "12.8"
            torch_ver: "2.9.1"
            tag_suffix: "py3.11-torch2.9.1-cuda12.8"
            filter_pattern: |
              src:
                - 'training/**'
          - service_name: training-ada
            context: ./training
            dockerfile: ./training/Dockerfile
            image_suffix: training
            cuda_ver: "12.4"
            torch_ver: "2.5.1"
            tag_suffix: "py3.10-torch2.5.1-cuda12.4"
            filter_pattern: |
              src:
                - 'training/**'
          - service_name: training-ampere
            context: ./training
            dockerfile: ./training/Dockerfile
            image_suffix: training
            cuda_ver: "11.8"
            torch_ver: "2.4.1"
            tag_suffix: "py3.10-torch2.4.1-cuda11.8"
            filter_pattern: |
              src:
                - 'training/**'
          - service_name: ui
            context: ./ui
            dockerfile: ./ui/Dockerfile
            image_suffix: ui
            tag_suffix: "latest"
            filter_pattern: |
              src:
                - 'ui/**'
          - service_name: mlflow
            context: ./mlflow
            dockerfile: ./mlflow/Dockerfile
            image_suffix: mlflow
            tag_suffix: "latest"
            filter_pattern: |
              src:
                - 'mlflow/**'
          - service_name: builder
            context: ./api
            dockerfile: ./api/Dockerfile.builder
            image_suffix: builder
            tag_suffix: "latest"
            filter_pattern: |
              src:
                - 'api/Dockerfile.builder'
                - 'api/deploy_worker.py'
                - 'api/pyproject.toml'
          - service_name: base
            context: ./api
            dockerfile: ./api/Dockerfile.base
            image_suffix: base
            tag_suffix: "latest"
            filter_pattern: |
              src:
                - 'api/Dockerfile.base'
                - 'api/service.py'
                - 'api/pyproject.toml'

    steps:
      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true
      
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Lowercase repository name
        run: |
          echo "REPO_LOWER=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV
      
      - name: Check for changes
        uses: dorny/paths-filter@v2
        id: changes
        with:
          filters: ${{ matrix.filter_pattern }}
      
      # 1. QEMU 설정
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # 2. Docker Buildx 설정
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        if: steps.changes.outputs.src == 'true' || github.event_name == 'workflow_dispatch'
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          # Dockerfile의 ARG로 매트릭스 변수 전달
          build-args: |
            TORCH_VER=${{ matrix.torch_ver }}
            CUDA_VER=${{ matrix.cuda_ver }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-${{ matrix.image_suffix }}:${{ matrix.tag_suffix }}
            ${{ matrix.service_name == 'training-ada' && format('{0}/{1}-{2}:latest', env.REGISTRY, env.REPO_LOWER, matrix.image_suffix) || '' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max