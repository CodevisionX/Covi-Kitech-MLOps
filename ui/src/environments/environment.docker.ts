export const environment = {
  production: true,
  // 브라우저가 접근할 주소입니다. 도커 네트워크 내부 이름이 아닌 
  // 실제 외부로 노출된 주소(localhost 또는 서버 IP)를 적어야 합니다.
  apiUrl: 'http://localhost:8000'  // 예: 'http://localhost:8000' 
};