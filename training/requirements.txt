ultralytics==8.2.0
mlflow==2.14.0
boto3
psycopg2-binary
dvc
dvc-s3

torchvision          # EfficientNet 모델 로드 및 이미지 변환(transforms) 필수
pandas               # MLflow 데이터 처리 및 결과 분석 시 자주 사용
opencv-python-headless  # 서버 환경(GUI 없는 도커)에서 OpenCV 충돌 방지용